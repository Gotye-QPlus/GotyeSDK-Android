<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/gotye_bg_pannel"
    android:gravity="center"
    android:orientation="horizontal" >

    <ImageButton android:id="@+id/gotye_pannel_face"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="fitXY"
        android:src="@drawable/gotye_btn_chat_pannel_face_selector"
        android:background="@android:color/transparent" />

    <ImageButton android:id="@+id/gotye_pannel_pic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/gotye_btn_chat_pannel_image_selector"
        android:background="@android:color/transparent"
        android:layout_marginLeft="10dip"
        android:layout_marginRight="10dip" />

    <ImageButton android:id="@+id/gotye_pannel_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/gotye_btn_chat_pannel_camera_selector"
        android:background="@android:color/transparent" />

</LinearLayout>